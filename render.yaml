services:
  - type: web
    name: minecraft-bot
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node bot.js
    envVars:
      - key: BOT_TOKEN
        fromDatabase: false
        value: "ضع التوكن هنا إذا لزم"
